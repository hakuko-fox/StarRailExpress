package io.wifi.starrailexpress.client.gui.screen;

/** Legacy selector fallback using the same destination artwork and departure animation. */
public final class MapVoteResultScreen extends MapVoteScreen {
    public MapVoteResultScreen(String mapId) {
        super();
        showResult(mapId);
    }
}
